#include <fstream>
#include "func.h"
#include "utils.h"
#include <iostream>
#include <string>


int main() {
		double **A;
		int n = 5;
		int iterations;
		A = createTriDiaMatrix(1, 2, n);

	return 0;
}
